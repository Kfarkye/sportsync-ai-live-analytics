# Shared (SSOT)

This package is the **single source of truth** for shared types, constants, and design tokens.

## Sync
Run:

```bash
npm run sync:shared
```

This will mirror canonical files into the app and Supabase runtime paths while keeping existing imports stable.
