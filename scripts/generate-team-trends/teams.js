/**
 * Legacy shim: canonical team metadata lives in functions/lib/teams.js.
 * Keep this file as a pass-through to avoid duplicate truth paths.
 */
export { NBA_TEAMS, teamByName, teamBySlug } from '../../functions/lib/teams.js';
