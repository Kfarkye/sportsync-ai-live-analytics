/**
 * Legacy shim: canonical trends computation lives in functions/lib/compute.js.
 * Keep this file as a pass-through to avoid duplicate truth paths.
 */
export { computeTeamStats } from '../../functions/lib/compute.js';
